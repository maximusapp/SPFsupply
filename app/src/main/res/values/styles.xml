<resources>

    <!-- Base application theme. -->
    <style name="AppTheme"
        parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay"
        parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" >
    <item name="android:windowContentOverlay">@null</item>

    </style>

    <style name="ToolbarColoredBackArrow" parent="AppTheme">
        <item name="android:textColorSecondary">@color/colorTitleToolBar</item>
    </style>

    <style name="ToolbarNewManufacturer" parent="AppTheme">
        <item name="android:textColorSecondary">@color/colorTitleToolBar</item>
        <item name="android:actionMenuTextColor">@color/colorTitleToolBar</item>
        <item name="homeAsUpIndicator">@mipmap/icon_back24dp</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:fontFamily">@font/latoregular</item>
        <item name="android:textSize">16sp</item>
    </style>

    <style name="SplashTheme"
        parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@drawable/background_splash</item>
    </style>

    <style name="MyCustomTextAppearanceTabLayout" parent="TextAppearance.Design.Tab">
        <item name="textAllCaps">false</item>
        <item name="android:fontFamily">@font/latoregular</item>
        <item name="android:textSize">18sp</item>
    </style>

    <style name="MyCustomSearchStyle" parent="Base.Widget.AppCompat.SearchView">
        <item name="queryBackground">@color/colorTitleToolBar</item>


    </style>

</resources>
